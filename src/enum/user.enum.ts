export enum VaccinationStatus {
  PENDING = "pending",
  COMPLETED = "completed",
}

export enum VaccinationDriveStatus {
  SCHEDULED = "scheduled",
  COMPLETED = "completed",
  CANCELLED = "cancelled",
}
